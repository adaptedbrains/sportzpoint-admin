/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'sportzpoint.s3.ap-south-1.amazonaws.com',
     
      'picsum.photos',
    ],
  },
};

export default nextConfig;
